import { FilterProfilsPipe } from './filter-profils.pipe';

describe('FilterProfilsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProfilsPipe();
    expect(pipe).toBeTruthy();
  });
});
